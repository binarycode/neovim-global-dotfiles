" number of spaces for each indent step
setlocal shiftwidth=4

" tab size for editing operations (like <Tab> and <BS>)
setlocal softtabstop=4

" mark column 80 with a vertical bar
setlocal colorcolumn=80
